<%= form_with(model: pqr) do |form| %>
  <% if pqr.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(pqr.errors.count, "error") %> prohibited this pqr from being saved:</h2>

      <ul>
        <% pqr.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div style="display:none;">
    <%= form.text_field :user_id, value: current_user.id %>
  </div>

  <div>
    <%= form.select :tipo, ['Consulta', 'Modificación', 'Problema de convivencia'], {:include_blank => true, prompt: 'Selecciona el tipo de pqr'}, required: true, style: "display: block" %>
  </div>
<br>
  <div>
    <%= form.label :descripcion, style: "display: block" %>
    <%= form.text_area :descripcion %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
